---
name: engrams-borrowed-sql-parameters-only
description: "Dynamic SQL parameter lists must borrow via Vec<&dyn ToSql>; boxing cloned parameters (Vec<Box<dyn rusqlite::ToSql>>) is prohibited — two allocations per parameter."
condition:
  - "Box<dyn rusqlite::ToSql>"
scope:
  - "tool:edit(src/ops/**)"
  - "tool:write(src/ops/**)"
interruptMode: always
---

# Borrowed SQL Parameters Only

Dynamic SQL parameter lists must borrow via Vec<&dyn ToSql>; boxing cloned parameters (Vec<Box<dyn rusqlite::ToSql>>) is prohibited — two allocations per parameter.

---

_Generated by engrams (pattern #7). Edit the pattern, not this file. Re-run `engrams rules export --harness omp` to refresh._
