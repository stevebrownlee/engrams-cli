---
name: engrams-no-throwaway-intermediate-collections
description: "Never materialize a collection only to consume it immediately; fuse iterators or pre-size with with_capacity. Detects collect::<Vec<_>>().join()."
---

# No Throwaway Intermediate Collections

Never materialize a collection only to consume it immediately; fuse iterators or pre-size with with_capacity. Detects collect::<Vec<_>>().join().

---

_Generated by engrams (pattern #10). Edit the pattern, not this file. Re-run `engrams rules export --harness omp` to refresh._
