---
name: engrams-one-allocation-per-string-chain
description: "Keep Cow<str> borrowed through string-transform chains; allocate once at the ownership boundary. Avoid from_utf8_lossy().trim().to_string()."
condition:
  - "from_utf8_lossy.*\\.to_string\\(\\)"
scope:
  - "tool:edit(src/ops/**)"
  - "tool:write(src/ops/**)"
interruptMode: never
---

# One Allocation Per String Chain

Keep Cow<str> borrowed through string-transform chains; allocate once at the ownership boundary. Avoid from_utf8_lossy().trim().to_string().

---

_Generated by engrams (pattern #8). Edit the pattern, not this file. Re-run `engrams rules export --harness omp` to refresh._
