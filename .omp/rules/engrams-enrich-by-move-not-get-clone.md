---
name: engrams-enrich-by-move-not-get-clone
description: "Consume temporary lookup maps with remove() or mem::take when enriching result rows; never clone out of a map that dies after the loop (map.get(&id).clone())."
condition:
  - "\\.get\\([^)]*\\)\\.clone\\(\\)"
scope:
  - "tool:edit(src/ops/**)"
  - "tool:write(src/ops/**)"
interruptMode: never
---

# Enrich By Move Not get().clone()

Consume temporary lookup maps with remove() or mem::take when enriching result rows; never clone out of a map that dies after the loop (map.get(&id).clone()).

---

_Generated by engrams (pattern #9). Edit the pattern, not this file. Re-run `engrams rules export --harness omp` to refresh._
