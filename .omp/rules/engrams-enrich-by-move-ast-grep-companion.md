---
name: engrams-enrich-by-move-ast-grep-companion
description: "Structural companion to the regex rule: catches .get(...).clone() enrich anti-patterns through method chains, whitespace, and multiline receivers that the line regex misses. The map dies after the loop - move out with remove()/mem::take instead of cloning."
astCondition:
  - "$MAP.get($KEY).clone()"
scope:
  - "tool:edit(src/**)"
  - "tool:write(src/**)"
interruptMode: never
---

# Enrich By Move (ast-grep companion)

Structural companion to the regex rule: catches .get(...).clone() enrich anti-patterns through method chains, whitespace, and multiline receivers that the line regex misses. The map dies after the loop - move out with remove()/mem::take instead of cloning.

---

_Generated by engrams (pattern #12). Edit the pattern, not this file. Re-run `engrams rules export --harness omp` to refresh._
