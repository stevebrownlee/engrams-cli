---
import DocsLayout from '../../layouts/DocsLayout.astro';
---

<DocsLayout title="Quick Start" description="Get Engrams running in your project in under five minutes — from zero to strategy-first retrieval.">
  <h1>Quick Start: Zero-Read Strategy in 5 Minutes</h1>
  <p class="lead">
    From an empty repository to a self-sufficient architecture graph in five commands.
    By the end, your AI assistant can plan features, verify contracts, and check Git drift without opening source files.
    Make sure you have completed the <a href="/docs/installation">installation</a> first.
  </p>

  <h2>Agent Setup</h2>
  <p>
    Engrams provides a built-in <code>instructions</code> command designed to output the standardized instruction manual that teaches LLM agents the Strategy-First retrieval protocol:
  </p>
  <pre><code>engrams instructions</code></pre>
  <p>
    Run this in your project root and paste the output directly into your <code>AGENTS.md</code> file, <code>.cursorrules</code>, or system prompt configuration.
  </p>

  <h2>5-Minute Walkthrough</h2>
  <div class="steps">

    <!-- Step 1 -->
    <div class="step">
      <div class="step-num">1</div>
      <div class="step-body">
        <h3>Initialize the local database</h3>
        <p>
          Create the embedded SQLite database in your workspace:
        </p>
        <pre><code>engrams init</code></pre>
      </div>
    </div>

    <!-- Step 2 -->
    <div class="step">
      <div class="step-num">2</div>
      <div class="step-body">
        <h3>Log an architecture decision with an explicit contract</h3>
        <p>
          When introducing an interface, behavior, or adapter, embed its contract so future sessions don't need to read implementation files:
        </p>
        <pre><code>engrams decision log \
  --summary "Provider behavior for payment adapters" \
  --contract "fn process(amount: u64, currency: &amp;str) -> Result&lt;Receipt, PaymentError&gt;" \
  --rationale "Pluggable payment processors across Stripe and PayPal" \
  --tags payment,adapter,api \
  --anchor src/adapters/stripe.rs</code></pre>
      </div>
    </div>

    <!-- Step 3 -->
    <div class="step">
      <div class="step-num">3</div>
      <div class="step-body">
        <h3>Rebuild the code graph</h3>
        <p>
          Let Engrams scan your repository, extracting exported symbols, docstrings, and line counts onto code nodes:
        </p>
        <pre><code>engrams graph rebuild</code></pre>
      </div>
    </div>

    <!-- Step 4 -->
    <div class="step">
      <div class="step-num">4</div>
      <div class="step-body">
        <h3>Retrieve full architectural context in one call</h3>
        <p>
          Before writing code, your agent runs <code>brief</code> to get the decision, contract, symbols, and Git staleness drift:
        </p>
        <pre><code>engrams brief "payment provider"</code></pre>
        <pre><code>&#123;
  "brief": &#123;
    "summary": "Provider behavior for payment adapters",
    "contract": "fn process(amount: u64, currency: &amp;str) -> Result&lt;Receipt, PaymentError&gt;",
    "drift": &#123; "stale": false, "checked": 1 &#125;
  &#125;,
  "neighbors": [
    &#123;
      "node": "code:42",
      "path": "src/adapters/stripe.rs",
      "symbols": ["StripeAdapter", "process", "refund"],
      "line_count": 148
    &#125;
  ]
&#125;</code></pre>
      </div>
    </div>

    <!-- Step 5 -->
    <div class="step">
      <div class="step-num">5</div>
      <div class="step-body">
        <h3>Close the session with PR verification</h3>
        <p>
          Enforce that PRs are anchored to decisions and record token ROI:
        </p>
        <pre><code>engrams session close --reads-skipped 12 --tokens-saved 60000 --pr 42</code></pre>
      </div>
    </div>
  </div>

  <h2>What to try next</h2>
  <div class="next-cards">
    <a href="/docs/for-agent/retrieving" class="next-card">
      <span class="nc-icon">⚡</span>
      <strong>Strategy-First Retrieval</strong>
      <span>How prime, brief, and cross-convention search save tokens</span>
    </a>
    <a href="/docs/for-agent/maintaining" class="next-card">
      <span class="nc-icon">📊</span>
      <strong>Telemetry &amp; PR Gates</strong>
      <span>Track zero-hit queries and enforce PR linkage</span>
    </a>
    <a href="/docs/for-you/exploration" class="next-card">
      <span class="nc-icon">🕸️</span>
      <strong>Interactive Graph</strong>
      <span>Explore the living 3D knowledge graph</span>
    </a>
    <a href="/docs/reference/cli" class="next-card">
      <span class="nc-icon">💻</span>
      <strong>CLI Reference</strong>
      <span>All commands, options, and flags</span>
    </a>
  </div>
</DocsLayout>

<style>
  .lead { font-size: 1.125rem; color: var(--color-text-muted); line-height: 1.75; margin-bottom: 1.75rem; }

  .steps { display: flex; flex-direction: column; gap: 0; }
  .step {
    display: flex; gap: 1.25rem;
    padding: 1.5rem 0;
    border-bottom: 1px solid var(--color-border);
  }
  .step:last-child { border-bottom: none; }
  .step-num {
    flex-shrink: 0;
    width: 2rem; height: 2rem;
    background: linear-gradient(135deg, var(--sky-aqua), var(--maya-blue));
    color: #fff; font-weight: 700; font-size: .875rem;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    margin-top: .1rem;
  }
  .step-body { flex: 1; }
  .step-body h3 { margin-top: 0; font-size: 1.05rem; }

  .next-cards {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(210px, 1fr));
    gap: 1rem;
    margin-top: 1rem;
  }
  .next-card {
    display: flex; flex-direction: column; gap: .35rem;
    background: var(--color-bg); border: 1.5px solid var(--color-border);
    border-radius: var(--radius-lg); padding: 1.25rem;
    text-decoration: none; color: var(--color-text);
    transition: border-color .15s, box-shadow .15s, transform .15s;
  }
  .next-card:hover { border-color: var(--sky-aqua); box-shadow: var(--shadow-md); transform: translateY(-2px); text-decoration: none; }
  .nc-icon { font-size: 1.4rem; }
  .next-card strong { font-size: .9375rem; }
  .next-card span { font-size: .8125rem; color: var(--color-text-muted); line-height: 1.5; }
</style>
