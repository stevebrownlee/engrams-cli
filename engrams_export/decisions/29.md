---
'@type': CreativeWork
identifier: "29"
title: Add /harden-rust omp prompt-template command for hardening LLM-written Rust
created: 2026-08-03T03:12:17Z
subject:
  - tooling
  - prompt-template
  - omp
  - workflow
  - rust
  - developer-experience
description: "Project-scoped omp prompt template at .omp/commands/harden-rust.md (filename => /harden-rust), discovered via the native slash-command provider (project commands live in <cwd>/.omp/commands/*.md per omp slash-command-internals). Three design choices: (1) Name harden-rust, verb-first; project-scoped (not user-global) because the charter persona rust-developer-persona.md ships in this repo. (2) Default scope = uncommitted working-tree .rs files via git status (i.e. what the agent just wrote), with --staged, --base <ref>, and explicit path/glob overrides. (3) Persona is the single source of truth: the body instructs the agent to read rust-developer-persona.md rather than embedding the charter, with a faithful inline audit grid (memory/zero-alloc, cache-friendly layout, static-vs-dynamic dispatch + #[inline], pet peeves) as a fast-reference distillation. Workflow is behavior-preserving: lsp references/rename before any signature/type change, fix at the source (no shims/allow), profiler-gated for hot-path micro-opts, verify with cargo fmt+build+clippy+targeted test, leave changes uncommitted for human review. Validated frontmatter parses (native commands fatal on parse error)."
---
# Add /harden-rust omp prompt-template command for hardening LLM-written Rust

```json
{
  "id": 29,
  "uuid": "97895955-4721-4423-8ce3-5da48cd9890f",
  "summary": "Add /harden-rust omp prompt-template command for hardening LLM-written Rust",
  "rationale": "Project-scoped omp prompt template at .omp/commands/harden-rust.md (filename => /harden-rust), discovered via the native slash-command provider (project commands live in <cwd>/.omp/commands/*.md per omp slash-command-internals). Three design choices: (1) Name harden-rust, verb-first; project-scoped (not user-global) because the charter persona rust-developer-persona.md ships in this repo. (2) Default scope = uncommitted working-tree .rs files via git status (i.e. what the agent just wrote), with --staged, --base <ref>, and explicit path/glob overrides. (3) Persona is the single source of truth: the body instructs the agent to read rust-developer-persona.md rather than embedding the charter, with a faithful inline audit grid (memory/zero-alloc, cache-friendly layout, static-vs-dynamic dispatch + #[inline], pet peeves) as a fast-reference distillation. Workflow is behavior-preserving: lsp references/rename before any signature/type change, fix at the source (no shims/allow), profiler-gated for hot-path micro-opts, verify with cargo fmt+build+clippy+targeted test, leave changes uncommitted for human review. Validated frontmatter parses (native commands fatal on parse error).",
  "implementation_details": "Filename harden-rust.md maps to /harden-rust. Frontmatter: description only (valid YAML; native commands fatal on parse error). Body sections: Arguments (4 scope modes), Steps (resolve scope, read persona, audit 4 bands, fix at source, verify cargo fmt/build/clippy/test, report), Rules (MUST/MUST NOT). Scope resolution: paths/globs via glob; --staged via git diff --staged --name-only --diff-filter=AM; --base <ref> via git diff --merge-base; default via git status --porcelain; keep only *.rs. No link target among existing 20 decisions (#14/#6/#23 are unrelated), so no relationship added.",
  "tags": [
    "tooling",
    "prompt-template",
    "omp",
    "workflow",
    "rust",
    "developer-experience"
  ],
  "timestamp": "2026-08-03T03:12:17Z",
  "status": "active",
  "commit_sha": "c8dc0528cc7103f5db0c9a9fd047c8ed789c89e2",
  "importance": 5,
  "access_count": 0,
  "last_accessed_at": null,
  "archived": 0,
  "contract": null
}
```
