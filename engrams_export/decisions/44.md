---
'@type': CreativeWork
identifier: "44"
title: "Path A ratified: TTSR-export-led policy engine; Path B demoted to prose-tier delivery"
created: 2026-08-05T04:43:41Z
subject:
  - architecture
  - guidance
  - omp
  - ttsr
  - policy-engine
description: "User ratified Path A (engrams as policy engine) on 2026-08-05, superseding the B-first sequencing recommendation from the 2026-08-04 design discussion. The original objection - engrams would compete with linters by building its own check engine - dissolves on omp because TTSR already is the engine (regex/ast-grep matching over edit/write streams, natively enforced mid-stream). Engrams' role: store, version, anchor, emit. Severity has no omp-native field, so it becomes export-time routing: error -> TTSR hard interrupt, warn -> hook advisory, info -> rulebook. Build order: (1) schema fields check_kind/check_expr/severity on system_patterns, (2) engrams rules export --harness omp generating .omp/rules/*.md, (3) engrams check --staged CI runner (ast-grep dependency decision lives here), (4) tool_call brief hook as the prose-tier follow-up. Warn-by-default is the trust discipline: only error severity hard-interrupts. The A/B fork closes: B is the delivery tier for what A cannot structurally check."
---
# Path A ratified: TTSR-export-led policy engine; Path B demoted to prose-tier delivery

```json
{
  "commit_sha": "0cc5680694871fedd6299d87169ca3e8c606e672",
  "id": 44,
  "implementation_details": null,
  "rationale": "User ratified Path A (engrams as policy engine) on 2026-08-05, superseding the B-first sequencing recommendation from the 2026-08-04 design discussion. The original objection - engrams would compete with linters by building its own check engine - dissolves on omp because TTSR already is the engine (regex/ast-grep matching over edit/write streams, natively enforced mid-stream). Engrams' role: store, version, anchor, emit. Severity has no omp-native field, so it becomes export-time routing: error -> TTSR hard interrupt, warn -> hook advisory, info -> rulebook. Build order: (1) schema fields check_kind/check_expr/severity on system_patterns, (2) engrams rules export --harness omp generating .omp/rules/*.md, (3) engrams check --staged CI runner (ast-grep dependency decision lives here), (4) tool_call brief hook as the prose-tier follow-up. Warn-by-default is the trust discipline: only error severity hard-interrupts. The A/B fork closes: B is the delivery tier for what A cannot structurally check.",
  "status": "active",
  "summary": "Path A ratified: TTSR-export-led policy engine; Path B demoted to prose-tier delivery",
  "tags": [
    "architecture",
    "guidance",
    "omp",
    "ttsr",
    "policy-engine"
  ],
  "timestamp": "2026-08-05T04:43:41Z",
  "uuid": "105f018e-1676-4226-a56e-7df4c629d31d"
}
```
