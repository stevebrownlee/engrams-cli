---
'@type': CreativeWork
identifier: "87"
title: Zero-cost hygiene pass on v0.13.0 schema hot paths (PR 5)
created: 2026-09-13T03:21:07Z
subject:
  - schemas
  - performance
  - review
description: Post-release persona review found the v0.13.0 loops allocation-clean but similarity/drift were O(n*m) contains() scans, kind.rs prepared statements per member, telemetry keyed by String ids, and one enrich-by-move violation (rule 004). Merge walks + hoisted statements + moved-out anchor winner remove all of it with byte-identical behavior; dogfood replay is the regression harness. dominant_tag max_by_key clone replaced with borrowing max_by (inverted comparator caught before ship). item_anchors index deliberately deferred to its own migration; NodeKey stringly seam deferred as a design change.
---
# Zero-cost hygiene pass on v0.13.0 schema hot paths (PR 5)

```json
{
  "id": 87,
  "uuid": "da736f1b-56ad-4814-a888-7c946c2b108a",
  "summary": "Zero-cost hygiene pass on v0.13.0 schema hot paths (PR 5)",
  "rationale": "Post-release persona review found the v0.13.0 loops allocation-clean but similarity/drift were O(n*m) contains() scans, kind.rs prepared statements per member, telemetry keyed by String ids, and one enrich-by-move violation (rule 004). Merge walks + hoisted statements + moved-out anchor winner remove all of it with byte-identical behavior; dogfood replay is the regression harness. dominant_tag max_by_key clone replaced with borrowing max_by (inverted comparator caught before ship). item_anchors index deliberately deferred to its own migration; NodeKey stringly seam deferred as a design change.",
  "implementation_details": null,
  "tags": [
    "schemas",
    "performance",
    "review"
  ],
  "timestamp": "2026-09-13T03:21:07Z",
  "status": "active",
  "commit_sha": "142c385b159a609a04801777347db8c8d310ca21",
  "importance": 5,
  "access_count": 0,
  "last_accessed_at": null,
  "archived": 0,
  "contract": "jaccard(a: &[&str], b: &[&str]) -> f64 and absent_count(a: &[&str], b: &[&str]) -> usize require ascending-sorted inputs; all callers satisfy it (member_strings sorts; confirmed_schemas ORDER BY yields key-string order)"
}
```
