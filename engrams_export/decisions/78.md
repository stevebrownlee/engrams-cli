---
'@type': CreativeWork
identifier: "78"
title: "Schema formation launch defaults resolved: gates density>=0.5/stability>=3/reward>=0; staging Jaccard 0.7 with drift-count logging; co-retrieval weight ramps by co-observation count; prime schema block K=3; assimilation fires at 0.4 token overlap, top-2"
created: 2026-09-05T01:34:56Z
subject:
  - schema-formation
  - launch-defaults
  - tuning
  - v0.13.0
description: "Reward floor 0 because retrieval_surfaces ships empty - any higher floor stalls formation on a signal the feature has not had time to collect; structure proposes first, reward takes over as telemetry accumulates. Jaccard 0.7 inherits the record-linkage calibration line; corrected drift math (J=(n-a)/(n+b)) shows additions (dominant mode in an accumulating KB) get a 43% budget, removals 30%, swaps ~18% per direction, so the expected failure is boundary-swap churn, not growth - and drift counts (last_drift_removed/added) are recorded on every staging match/miss so phase-1 tuning diagnoses reset causes instead of nudging thresholds blind. Co-retrieval weight scales by co-observation count through the same normalize-and-cap curve as co-commit: empty telemetry yields zero edges by construction, no schedule constant, no wall-clock in the deterministic scan (decision 72 invariant). Prime K=3 compact lines keeps the token covenant while making schemas discoverable and reward-observable. Assimilation 0.4/top-2 balances precision against thin early centroids; suggestion statuses accumulate as calibration data for v0.14 adaptation. All five remain sweep constants owned by the phase-1 dogfood gate."
---
# Schema formation launch defaults resolved: gates density>=0.5/stability>=3/reward>=0; staging Jaccard 0.7 with drift-count logging; co-retrieval weight ramps by co-observation count; prime schema block K=3; assimilation fires at 0.4 token overlap, top-2

```json
{
  "access_count": 0,
  "archived": 0,
  "commit_sha": "784380ac2cb11edba1472d177b07f5a44dc7b8d5",
  "contract": null,
  "id": 78,
  "implementation_details": null,
  "importance": 5,
  "last_accessed_at": null,
  "rationale": "Reward floor 0 because retrieval_surfaces ships empty - any higher floor stalls formation on a signal the feature has not had time to collect; structure proposes first, reward takes over as telemetry accumulates. Jaccard 0.7 inherits the record-linkage calibration line; corrected drift math (J=(n-a)/(n+b)) shows additions (dominant mode in an accumulating KB) get a 43% budget, removals 30%, swaps ~18% per direction, so the expected failure is boundary-swap churn, not growth - and drift counts (last_drift_removed/added) are recorded on every staging match/miss so phase-1 tuning diagnoses reset causes instead of nudging thresholds blind. Co-retrieval weight scales by co-observation count through the same normalize-and-cap curve as co-commit: empty telemetry yields zero edges by construction, no schedule constant, no wall-clock in the deterministic scan (decision 72 invariant). Prime K=3 compact lines keeps the token covenant while making schemas discoverable and reward-observable. Assimilation 0.4/top-2 balances precision against thin early centroids; suggestion statuses accumulate as calibration data for v0.14 adaptation. All five remain sweep constants owned by the phase-1 dogfood gate.",
  "status": "active",
  "summary": "Schema formation launch defaults resolved: gates density>=0.5/stability>=3/reward>=0; staging Jaccard 0.7 with drift-count logging; co-retrieval weight ramps by co-observation count; prime schema block K=3; assimilation fires at 0.4 token overlap, top-2",
  "tags": [
    "schema-formation",
    "launch-defaults",
    "tuning",
    "v0.13.0"
  ],
  "timestamp": "2026-09-05T01:34:56Z",
  "uuid": "2c5b6e89-3854-4b3b-b024-943a038899db"
}
```
